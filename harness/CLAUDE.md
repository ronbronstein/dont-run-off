@AGENTS.md

## Claude Code

- Confirm what actually loaded with `/context` — don't assume these instructions are in play.
- If my instructions seem to be getting ignored, suspect conflicting sources before assuming the model is being difficult. Check `/context` for other loaded files, and `/doctor` if the rules have grown large.
